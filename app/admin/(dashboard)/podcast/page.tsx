import SectionList from "../../_components/SectionList";
import { sections } from "../../_lib/sections";

export default function SectionListPage() {
  return <SectionList section={sections.podcast} />;
}
